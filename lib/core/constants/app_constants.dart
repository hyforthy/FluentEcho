class AppConstants {
  static const Duration llmTimeout = Duration(seconds: 10);
  static const Duration ttsTimeout = Duration(seconds: 30);
  static const Duration sttTimeout = Duration(seconds: 30);
  static const int notePageSize = 20;
  static const String audioSubDirectory = 'audio';
}
